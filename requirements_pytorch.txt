# CardioEquation V2: PyTorch-based DiT-ECG System
# ================================================
# Install with: pip install -r requirements_pytorch.txt

# Core PyTorch (install via https://pytorch.org for CUDA-specific version)
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# Numerical Computing & Signal Processing
numpy>=1.21.0
scipy>=1.7.0

# ECG-Specific Libraries
neurokit2>=0.2.0              # ECG peak detection, delineation, morphological analysis
wfdb>=4.0.0                   # PhysioNet WFDB for MIT-BIH, PTB-XL download

# Data Handling
pandas>=1.3.0
scikit-learn>=1.0.0

# ECG Digitizer (clinical validation)
PyMuPDF>=1.22.0                 # PDF to image conversion (import fitz)
opencv-python-headless>=4.6.0   # Image processing for ECG trace extraction

# Visualization
matplotlib>=3.5.0

# Training Utilities
tqdm>=4.62.0
einops>=0.7.0                 # Tensor rearrangement utilities

# Optional: Advanced Training
# flash-attn>=2.3.0           # FlashAttention (H100/A100 only, install separately)
# deepspeed>=0.12.0           # ZeRO optimizer for multi-GPU
# wandb>=0.15.0               # Experiment tracking

# Legacy TF Dependencies (for loading old Phase 1-3 weights if needed)
# tensorflow>=2.8.0
