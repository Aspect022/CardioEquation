# ── Python ──────────────────────────────────
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
*.egg
data/
*.npz
*.pth
checkpoints/
# ── Virtual Environments ────────────────────
venv/
venv_v2/
.venv/
env/

# ── IDE ─────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── Data (large, downloaded on GPU server) ──
data/mitbih_raw/
data/mitdb/
data/ptbxl/
data/cpsc2018/
data/mitbih_forecasting.npz
data/ptbxl_processed.npz
data/real_ecg_dataset.npy
data/realistic_train.npz
data/verification_report.json
checkpoints/*.pt
checkpoints/*.pth
*.h5
*.weights.h5
*.ckpt
# Keep training config and logs (small files)
!checkpoints/train_config.json
!checkpoints/training_log.txt



# ── Model outputs (keep validation results) ──
outputs/**/*.pt
outputs/**/*.pth
# Keep validation reports and plots
!outputs/clinical_validation/
!outputs/clinical_validation/**

# ── OS files ────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs ────────────────────────────────────
*.log
wandb/

# ── Keep tracked ────────────────────────────
# NOTE: Dataset/ folder with hospital PDFs is tracked
# NOTE: src/ code is tracked
# NOTE: docs/ is tracked
# NOTE: models/ (model architecture code) is tracked — NOT weights
